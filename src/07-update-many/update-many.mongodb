use("sample_training");

db.zips.find({ city: "CLEVELAND" }).count();
db.zips.updateMany(

  { city: "CLEVELAND" },               //Query principal, o seleccionador de atributo

  {
    $inc: {                            //Operador incrementador
      pop: 1000
    }
  }
)