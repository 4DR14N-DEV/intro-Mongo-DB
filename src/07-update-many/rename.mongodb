use("sample_training");

db.zips.updateMany(

  { city: "CLEVELAND" },               //Query principal, o seleccionador de atributo

  {
    $rename: {                            //Operador para renombrar atributos
      myAttr: "My_data"
    }
  }
)